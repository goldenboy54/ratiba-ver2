<%- include('partials/nav') %>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Programs Management</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />

  <style>
    .table-container { overflow-x: auto; margin-top: 20px; }
    th { text-align: center; background-color: #007bff; color: #fff; }
    td { text-align: center; vertical-align: middle; }
    .btn-download { margin: 5px; }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Programs Management</h1>

    <!-- Add Program Button -->
    <a href="/programs/form" class="btn btn-primary mb-3"><i class="fas fa-plus"></i> Add Program</a>

    <!-- Display Programs -->
    <% if (locals.Viewprograms) { %>
        <!-- Search Form -->
        <form method="get" action="/programs" id="filterForm" class="mb-4">
          <div class="form-row">
            <div class="form-group col-md-4">
              <label for="program_name">program_name:</label>
              <select id="program_name" name="program_name" class="form-control select2">
                <option value="">program_name</option>
                <% pname.forEach(item => { %>
                  <option value="<%= item.program_name %>" <%= locals.query?.program_name === item.program_name ? 'selected' : '' %>><%= item.program_name %></option>
                <% }); %>
              </select>
            </div>
    
            <div class="form-group col-md-4">
              <label for="program_code">program_code:</label>
              <select id="program_code" name="program_code" class="form-control select2">
                <option value="">program_code</option>
                <% pcode.forEach(item => { %>
                  <option value="<%= item.program_code %>" <%= locals.query?.program_code === item.program_code ? 'selected' : '' %>><%= item.program_code %></option>
                <% }); %>
              </select>
            </div>
    
            <div class="form-group col-md-4">
              <label for="duration">duration:</label>
              <select id="duration" name="duration" class="form-control select2">
                <option value="">duration</option>
                <% pduration.forEach(item => { %>
                  <option value="<%= item.duration %>" <%= locals.query?.duration === item.duration ? 'selected' : '' %>><%= item.duration %></option>
                <% }); %>
              </select>
            </div>
  
            <div class="form-group col-md-4">
              <label for="level">level:</label>
              <select id="level" name="level" class="form-control select2">
                <option value="">level</option>
                <% plevel.forEach(item => { %>
                  <option value="<%= item.level %>" <%= locals.query?.level === item.level ? 'selected' : '' %>><%= item.level %></option>
                <% }); %>
              </select>
            </div>
    
            <div class="form-group col-md-4">
              <label for="category">category:</label>
              <select id="category" name="category" class="form-control select2">
                <option value="">category</option>
                <% pcategory.forEach(item => { %>
                  <option value="<%= item.category %>" <%= locals.query?.category === item.category ? 'selected' : '' %>><%= item.category %></option>
                <% }); %>
              </select>
            </div>
    
            <div class="form-group col-md-4">
              <label for="program_capacity">program_capacity:</label>
              <select id="program_capacity" name="program_capacity" class="form-control select2">
                <option value="">program_capacity</option>
                <% pcapacity.forEach(item => { %>
                  <option value="<%= item.program_capacity %>" <%= locals.query?.program_capacity === item.program_capacity ? 'selected' : '' %>><%= item.program_capacity %></option>
                <% }); %>
              </select>
            </div>

            <div class="form-group col-md-4">
              <label for="program_type">program_type:</label>
              <select id="program_type" name="program_type" class="form-control select2">
                <option value="">program_type</option>
                <% ptype.forEach(item => { %>
                  <option value="<%= item.program_type %>" <%= locals.query?.program_type === item.program_type ? 'selected' : '' %>><%= item.program_type %></option>
                <% }); %>
              </select>
            </div>
          </div>
          <div class="form-group">
            <button type="submit" class="btn btn-primary btn-block">Filter</button>
          </div>
        </form>

      <!-- CSV/PDF/Print Download Buttons -->
      <div class="text-center mb-3">
        <button id="csvDownloadBtn" class="btn btn-success btn-download">Download CSV</button>
        <button id="pdfDownloadBtn" class="btn btn-info btn-download">Download PDF</button>
        <button id="printBtn" class="btn btn-secondary btn-download">Print</button>
      </div>

      <h2 class="mb-4">All Programs</h2>
      <div class="table-container" id="programTableContainer">
        <table class="table table-bordered table-striped" id="programTable">
          <thead class="thead-dark">
            <tr>
              <th title="EXAMPLE COMPUTER SCIENCE">Program Name</th>
              <th>Program Code</th>
              <th title="MEANS NUMBER OF YEAR FOR PROGRAM">Program Duration</th>
              <th>Level</th>
              <th title="MEANS PROGRAM DEPARTMENT">Department</th>
              <th>Program Capacity</th>
              <th  title="MAY BE FULL TIME OR EVENING">Program Type</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if (Viewprograms.length > 0) { %>
              <% Viewprograms.forEach(program => { %>
                <tr>
                  <td><%= program.program_name %></td>
                  <td><%= program.program_code %></td>
                  <td><%= program.duration %></td>
                  <td><%= program.level %></td>
                  <td><%= program.category %></td>
                  <td><%= program.program_capacity %></td>
                  <td><%= program.program_type %></td>
                  <td>
                    <button type="button" class="btn btn-warning btn-sm" data-toggle="modal" data-target="#updateModal<%= program.program_id %>">Edit</button>
                    <a href="/programs/delete/<%= program.program_id %>" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this program?');">Delete</a>
                  </td>
                </tr>

                <!-- Update Modal -->
                <div class="modal fade" id="updateModal<%= program.program_id %>" tabindex="-1" role="dialog" aria-labelledby="updateModalLabel<%= program.program_id %>" aria-hidden="true">
                  <div class="modal-dialog" role="document">
                    <div class="modal-content">
                      <div class="modal-header">
                        <h5 class="modal-title" id="updateModalLabel<%= program.program_id %>">Update Program</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                          <span aria-hidden="true">&times;</span>
                        </button>
                      </div>
                      <div class="modal-body">
                        <form action="/programs/edit/<%= program.program_id %>" method="post">
                          <div class="form-group">
                            <label for="name"><p class="text-info">Program Name:</p></label>
                            <input type="text" id="name" name="name" class="form-control" value="<%= program.program_name %>" required>
                          </div>
                          <div class="form-group">
                            <label for="program_code"  class="text-info">Program Code:</label>
                            <input type="text" id="program_code" name="program_code" class="form-control " value="<%= program.program_code %>" required>
                          </div>
                          <div class="form-group">
                            <label for="duration"  class="text-info">Duration:</label>
                            <input type="text" id="duration" name="duration" class="form-control " value="<%= program.duration %>" required>
                          </div>
                          <div class="form-group">
                            <label for="level"  class="text-info">Level:</label>
                            <input type="text" id="level" name="level" class="form-control " value="<%= program.level %>" required>
                          </div>
                          <div class="form-group">
                            <label for="category" class="text-info">Department:</label>
                            <select id="category" name="category" class="form-control select2" required>
                              <% departments.forEach(department => { %>
                                <option value="<%= department.department_name %>" <% if (program.category === department.department_name) { %> selected <% } %>><%= department.department_name %></option>
                              <% }); %>
                            </select>
                          </div>
                          <div class="form-group">
                            <label for="program_capacity"  class="text-info">Program Capacity:</label>
                            <input type="text" id="program_capacity" name="program_capacity" class="form-control" value="<%= program.program_capacity %>">
                          </div>
                          <div class="form-group">
                            <label for="program_type"  class="text-info">program_type:</label>
                            <select id="program_type" name="program_type" class="form-control select2" required>
                              <option value="full-time" <%= program.program_type === 'full-time' ? 'selected' : '' %>>full-time</option>
                              <option value="evening" <%= program.program_type === 'evening' ? 'selected' : '' %>>evening</option>
                              <option value="veta" <%= program.program_type === 'veta' ? 'selected' : '' %>>veta</option>
                            </select>
                          </div>
                          <button type="submit" class="btn btn-success">Submit Changes</button>
                        </form>
                      </div>
                    </div>
                  </div>
                </div>
              <% }) %>
            <% } %>
          </tbody>
        </table>
      </div>
    
  <% } else { %>
    <!-- Add Program Form & Upload -->
    <h2 class="mb-4">Add Program</h2>
    <!-- CSV Upload form -->
    <h3>Upload CSV/Excel File</h3>
    <form action="/programs/uploads" method="post" enctype="multipart/form-data">
      <div class="form-group">
        <label for="file">Choose CSV/Excel File:</label>
        <input type="file" name="file" class="form-control" required>
      </div>
      <button type="submit" class="btn btn-success">Upload</button>
    </form>
    <form action="/programs" method="post">
      <div class="form-group">
        <label for="name">Program Name:</label>
        <input type="text" id="name" name="name" class="form-control" required>
      </div>
      <div class="form-group">
        <label for="program_code">Program Code:</label>
        <input type="text" id="program_code" name="program_code" class="form-control" required>
      </div>
      <div class="form-group">
        <label for="duration">Duration:</label>
        <input type="text" id="duration" name="duration" class="form-control" required>
      </div>
      <div class="form-group">
        <label for="level">Level:</label>
        <input type="text" id="level" name="level" class="form-control" required>
      </div>
      <div class="form-group">
        <label for="category">Department:</label>
        <select id="category" name="category" class="form-control select2" required>
          <% departments.forEach(department => { %>
            <option value="<%= department.department_name %>"><%= department.department_name %></option>
          <% }); %>
        </select>
      </div>
      <div class="form-group">
        <label for="program_capacity">Program Capacity:</label>
        <input type="text" id="program_capacity" name="program_capacity" class="form-control">
      </div>
      <div class="form-group">
        <label for="program_type">program_type:</label>
        <select id="program_type" name="program_type" class="form-control select2" required>
          <option value="full-time">full-time </option>
          <option value="evening">evening</option>
          <option value="veta">veta</option>
        </select>
      </div>
      <button type="submit" class="btn btn-success">Add Program</button>
    </form>
  <% } %>
  </div>

<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>

<script>
$(document).ready(function(){
  $('.select2').select2();

  $('#filterForm select').on('change', function(){ $('#filterForm').submit(); });

  function getFileName(type){
    const now = new Date();
    const timestamp = now.getFullYear()+'-'+(now.getMonth()+1)+'-'+now.getDate()+'_'+now.getHours()+'-'+now.getMinutes();
    let filterStr = '';
    $('#filterForm select').each(function(){
      if($(this).val()) filterStr += $(this).val()+'_';
    });
    filterStr = filterStr ? filterStr.slice(0,-1) : 'all';
    return `ATC_${filterStr}_${timestamp}.${type}`;
  }

  $('#csvDownloadBtn').click(function(){
    const rows = document.querySelectorAll("#programTable tr");
    let csv = [];
    rows.forEach(row=>{
      let cols = row.querySelectorAll("td, th");
      let rowData = [];
      cols.forEach(col=>rowData.push('"' + col.innerText + '"'));
      csv.push(rowData.join(","));
    });
    const csvFile = new Blob([csv.join("\n")], {type:"text/csv"});
    const a = document.createElement("a");
    a.href = URL.createObjectURL(csvFile);
    a.download = getFileName('csv');
    a.click();
  });

  $('#printBtn').click(function(){
    const content = document.getElementById('programTableContainer').innerHTML;
    const win = window.open('', '', 'height=700,width=900');
    win.document.write('<html><head><title>Programs Print</title></head><body>');
    win.document.write(content);
    win.document.write('</body></html>');
    win.document.close(); win.focus(); win.print();
  });

  $('#pdfDownloadBtn').click(function(){
    const { jsPDF } = window.jspdf;
    const doc = new jsPDF('l', 'pt', 'a4');
    const table = document.getElementById('programTableContainer');
    const pageHeight = doc.internal.pageSize.getHeight();
    const pageWidth = doc.internal.pageSize.getWidth();

    html2canvas(table, { scale:2 }).then(canvas=>{
      const imgData = canvas.toDataURL('image/png');
      const imgProps = doc.getImageProperties(imgData);
      const pdfHeight = (imgProps.height * pageWidth)/imgProps.width;

      let heightLeft = pdfHeight;
      let position = 0;

      doc.addImage(imgData, 'PNG', 0, position, pageWidth, pdfHeight);
      heightLeft -= pageHeight;

      while(heightLeft > 0){
        position = heightLeft - pdfHeight;
        doc.addPage();
        doc.addImage(imgData, 'PNG', 0, position, pageWidth, pdfHeight);
        heightLeft -= pageHeight;
      }
      doc.save(getFileName('pdf'));
    });
  });

});
</script>

<%- include('partials/footer') %>
</body>
</html>
