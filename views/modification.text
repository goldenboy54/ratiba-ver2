CREATE TABLE timetable_history (
    id INT AUTO_INCREMENT PRIMARY KEY,
    timetable_id INT NOT NULL,
    swapped_with INT,
    day VARCHAR(20),
    start_time TIME,
    end_time TIME,
    subject_code TEXT,
    subject_name TEXT,
    venue_name VARCHAR(255),
    tutor_name VARCHAR(255),
    venue_location VARCHAR(255),
    department_name VARCHAR(255),
    program_name TEXT,
    subject_credit INT,
    program_level TEXT,
    venue_type TEXT,
    venue_status VARCHAR(255),
    year TEXT,
    total_hours_per_week INT DEFAULT 0,
    program_type VARCHAR(255),
    venue_id INT,
    semester VARCHAR(255) NOT NULL,
    venue_capacity INT,
    program_capacity TEXT,
    arrange INT DEFAULT 1,
    created_by VARCHAR(100),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_by VARCHAR(100),
    updated_at DATETIME DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
    program_code TEXT,
    snapshot_at DATETIME DEFAULT CURRENT_TIMESTAMP
);


CHECKUP
1. all free venues must be seen and its time to be shown crearly 
     "solved"
2. subject in pair
   "solved"

3. subject to be assigned in their specific venue 
for Lab to be assigned on Lab venue of same department as subject
   "solved"

4. Collision test
   "to ensure no collision of same user or same program in same time slot"
      "solved"
      
5. To ensure each subject assigned number of periog according to its total hours per week
  "done"




<!-- partials/nav.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Timetable Management System</title>

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Font Awesome Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>
  <!-- <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" /> -->

  <style>
    body {
      background: linear-gradient(to right, #e0eafc, #cfdef3);
      min-height: 100vh;
      display: flex;
      overflow-x: hidden;
      color: #333;
    }

    .table-container {
      overflow-x: auto; /* Enables horizontal scrolling if table overflows */
    }

    .key {
      font-weight: bold;
      color: #e91e63;
    }
    .slot {
      padding: 5px;
      margin: 5px 0;
      border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    }
    .sidebar {
      min-width: 250px;
      max-width: 250px;
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      color: white;
      padding: 20px 0;
      position: fixed;
      height: 100vh;
      overflow-y: auto;
      box-shadow: 2px 0 5px rgba(0,0,0,0.1);
    }

    .sidebar a {
      color: white;
      padding: 15px 25px;
      display: flex;
      align-items: center;
      font-size: 16px;
      text-decoration: none;
      transition: all 0.3s;
    }

    .sidebar a:hover {
      background-color: rgba(255, 255, 255, 0.2);
      text-decoration: none;
      border-left: 4px solid #fff;
    }

    .sidebar a i {
      margin-right: 10px;
      font-size: 18px;
    }

    .main-content {
      margin-left: 250px;
      padding: 20px;
      width: 100%;
      animation: fadeIn 1s ease-in;
    }

    .logo {
      text-align: center;
      margin-bottom: 30px;
    }

    .logo img {
      width: 120px;
      border-radius: 50%;
      border: 3px solid white;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Responsive Sidebar */
    @media (max-width: 768px) {
      .sidebar {
        position: relative;
        height: auto;
        width: 100%;
      }
      .main-content {
        margin-left: 0;
      }
    }

  </style>
</head>
<body>



 <!-- sidebar.ejs -->
<div class="sidebar">
  <div class="logo">
    <a href="/dashboard">
      <img src="/images/atc.jpg" alt="ATC Logo" class="img-fluid" />
    </a>
  </div>

  <% if (user && (user.role === "tmaster" || user.role === "admin")) { %>
      <a href="/timetables"><i class="fas fa-cogs"></i> Settings</a>
      <!-- updateTimetable.ejs -->
      <a href="/manageTimetable"><i class="fas fa-edit"></i> Mix Programs in Timetable</a>
      <a href="/manageTimetable/freed-slots"><i class="fas fa-clock"></i> Freed Slots</a>
          <a href="/fillFreedSlots"><i class="fas fa-plus-circle"></i> Fill Freed Slots</a>
      <a href="/tmaster"><i class="fas fa-magic"></i> Auto Generate Timetable <i class="text-warning">Recommended - time saving</i></a>
      <a href="/manualtimetable"><i class="fas fa-table"></i> Manual Generate Timetable <i class="text-warning">Not Recommended - time consuming</i></a>
      <a href="/programs"><i class="fas fa-graduation-cap"></i> Programs</a>
      <a href="/venues"><i class="fas fa-map-marker-alt"></i> Venues</a>
    <a href="/users"><i class="fas fa-users"></i> Tutors</a>
        <a href="/registered_subjects"><i class="fas fa-book"></i> Modules</a>
    <a href="/work_reports"><i class="fas fa-clipboard-list"></i> Tutor Work Load</a>
        <a href="/departments"><i class="fas fa-building"></i> Departments</a>
   <a href="/subjects"><i class="fas fa-book-open"></i> Allocated Modules By HODS</a>

  <% } %>

  <% if ( user && user.role === "hod") { %>
      <!-- updateTimetable.ejs -->
      <a href="/programs"><i class="fas fa-graduation-cap"></i> Programs</a>
    <a href="/subjects"><i class="fas fa-book-open"></i> Allocate Modules</a>
    <a href="/registered_subjects"><i class="fas fa-book"></i> Modules</a>
    <a href="/departments"><i class="fas fa-building"></i> Departments</a>
    <a href="/venues"><i class="fas fa-map-marker-alt"></i> Venues</a>
    <a href="/users"><i class="fas fa-users"></i> Tutors</a>
     <a href="/manageTimetable"><i class="fas fa-edit"></i> Mix Programs in Timetable</a>
      <a href="/manageTimetable/freed-slots"><i class="fas fa-clock"></i> Freed Slots</a>
    <a href="/fillFreedSlots"><i class="fas fa-plus-circle"></i> Fill Freed Slots</a>
  <% } %>

  <% if ( user && (user.role === "student" || user.role === "tutor" || user.role === "hod" || user.role === "tmaster")) { %>
    <a href="/searchtimetable"><i class="fas fa-calendar-alt"></i> View Timetable</a>
  
  <a href="/profile"><i class="fas fa-cogs"></i> Profile Settings</a>

  <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>

    <% } %>

    <!-- <--if not logged in -->
     <% if (!user){ %>
         <a href="/"><i class="fas fa-sign-out-alt"></i>Back</a>
   <%  } %>

  
  
</div>


  <div class="main-content">




<%- include('partials/nav') %>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Manual Generate timetable</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/css/select2.min.css" />
  <style>
    .table-container {
      overflow-x: auto; /* Ensures horizontal scrolling if table overflows */
    }
    .animated-graphic {
      font-size: 3rem; /* Adjust as needed */
      animation: spin 3s linear infinite; /* Add a spinning animation */
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  
    .table th, .table td {
      vertical-align: middle;
      text-align: center;
      font-size: 1.1rem;
    }
 
  </style>
  <style>
 
    .fixed-size-btn {
      width: 80px;
      height: 35px;
    }
  
    .modal-dialog {
      max-width: 90%;
    }
  
    /* Responsive button size */
    @media (max-width: 768px) {
      .fixed-size-btn {
        width: 100px;
        height: 40px;
      }
    }
  

  

   
  </style>
  
</head>
<body>

  <div class="container mt-5">
    <div class="text-center mb-4">
      <div class="animated-graphic" title="Timetable autogenerating system">🕑</div> 
    </div> 

    <h2 class="text-center text-primary">FILTER BELOW TO CONTINUE</h2>

<% if (typeof error !== 'undefined' && error) { %>
  <div class="alert alert-danger alert-dismissible fade show" role="alert">
    <%= error %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% } %>

<% if (typeof success !== 'undefined' && success) { %>
  <div class="alert alert-success alert-dismissible fade show" role="alert">
    <%= success %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% } %>

<% if (logs && logs.length > 0) { %>
  <ul style="color:blue;">
    <% logs.forEach(l => { %>
      <li><%= l %></li>
    <% }) %>
  </ul>
<% } %>


    <!-- Search Form -->
    <form method="get" action="/manualTimetable" class="mb-4">

     <div class="form-row">
        <div class="form-group col-md-4">
          <label for="tutor">Tutor:</label>
          <select id="tutor" name="tutor" class="form-control select2">
            <option value="">Select Tutor</option>
            <% tutors.forEach(tutor => { %>
              <option value="<%= tutor.full_name %>" <%= tutor_name === tutor.full_name ? 'selected' : '' %>><%= tutor.full_name %></option>
            <% }); %>
          </select>
        </div>
        
        <div class="form-group col-md-4">
          <label for="semester">Semester:</label>
          <select id="semester" name="semester" class="form-control select2">
            <option value="">Select semester</option>
            <% semesters.forEach(semester => { %>
              <option value="<%= semester.semester %>" <%= semester === semester.semester ? 'selected' : '' %>> <%= semester.semester %></option>
            <% }); %>
          </select>
        </div>

     
     <div class="form-group col-md-4">
      <label for="program_type">program_type:</label>
      <select id="program_type" name="program_type" class="form-control select2">
        <option value="">Select type</option>
        <% ptypes.forEach(type => { %>
          <option value="<%= type.program_type %>" <%= program_type === type.program_type ? 'selected' : '' %>><%= type.program_type %></option>
        <% }); %>
      </select>
    </div>
     </div>
      <button type="submit" class="btn btn-primary">Filter</button>
    </form>
<br>
    <!-- Display Timetables -->
    <% if ( tutor_name ) { %> 
    
     <% if (timetables.length > 0) { %>
  
<br>
        <p class="text-primary text-center">ARUSHA TECHNICAL COLLEGE</p>
       
        <p class="text-success text-center">TIMETABLE GENERATION</p>
        <div class="table-container">
          <table class="table table-bordered table-striped">
            <thead>
              <tr>
               
               <form action="/manualTimetable/add" method="post">

                    <div class="form-group">
                         <label for="day">day:</label>
                         <select id="day" name="day" class="form-control select2" required>
                        
                              <option value="MONDAY">MONDAY</option>
                           <option value="TUESDAY">TUESDAY</option>    
                           <option value="WEDNESSDAY">WEDNESSDAY</option>
                           <option value="THURSDAY">THURSDAY</option>    
                           <option value="FRIDAY">FRIDAY</option>
                           <option value="SATURDAY">SATURDAY</option>   
                           <option value="SUNDAY">SUNDAY</option>
      
                         </select>
                       </div>
                       <label for="venue">Venue Selection:</label>
                       <select id="venue_id" name="venue_id" class="form-control select2" required>
                         <% venues.forEach(venue => { %>
                          <mark>
                           <option value="<%= venue.venue_id %>">
                             <mark>Vname: <%= venue.venue_name %> Vcapacity : <%= venue.capacity %></mark>   Vtype: <%= venue.department %> </mark> <%= venue.type %></mark>
                           </option>                           
                          </mark>


                         <% }); %>
                       </select>
                       
                       
                       <div class="form-group">
                         <label for="subject_ids">Assign to:</label>
                     <select id="subject_ids" name="subject_ids[]" class="form-control select2" multiple required>
  
                           <% timetables.forEach(timetable => { %>
                             <option value="<%= timetable.subject_id %>">
                               <mark> <%= timetable.tutor_name %></mark>
                               (     Subject <mark><%= timetable.subject_name %></mark>, Scode: <%= timetable.subject_code %>, Stype: <%= timetable.department_name %>  <%= timetable.subject_type %>,ltpa: <%= timetable.total_hours_per_week %> hours ,pname: <%= timetable.program_name %>,ptype: <%= timetable.program_type %>,Pcapacity : <%= timetable.program_capacity %>, dname: <%= timetable.subject_department %> semester <%= timetable.semester %>,)
                             </option>
                           <% }); %>
                         </select>
                       </div>
                       
     

                 <button type="submit" class="btn btn-primary">Assign</button>
               </form>
     
     
     
          
     
              </tr>
            </thead>
            <tbody>
               <p style="color: chartreuse;">Number of programs Assigned to that tutor </p>
               
               <%
              x=1;
              timetables.forEach(timetable => { %>
                <tr>
                    <mark style="color: chartreuse;">

                    <%= x  %> Program   
               </mark>
                </tr>

                <% x++  %>
                <!-- Update Modal -->
                <div class="modal fade" id="updateModal<%= timetable.id %>" tabindex="-1" role="dialog" aria-labelledby="updateModalLabel<%= timetable.id %>" aria-hidden="true">
                  <div class="modal-dialog" role="document">
                    <div class="modal-content">
                      <div class="modal-header">
                        <h5 class="modal-title" id="updateModalLabel<%= timetable.id %>">Update Timetable</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                          <span aria-hidden="true">&times;</span>
                        </button>
                      </div>
                      <div class="modal-body">
                        <!-- Update form content here -->
                      </div>
                    </div>
                  </div>
                </div>


              <% }); %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <p>No timetables found for the selected filters.</p>
      <% } %>
    <% } else { %>
      <p>Please select filters to view timetables.</p>
    <% } %>

  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/js/select2.min.js"></script>
  <script>
    $(document).ready(function() {
      $('.select2').select2();
    });
  </script>
</body>
</html>



